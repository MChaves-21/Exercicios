// babel.config.js

module.exports = { // Exporta um objeto de configuração para o Babel
    presets: [ // Define os presets a serem usados
      ['@babel/preset-env'] // Preset que permite transpilar JavaScript moderno para versões compatíveis com navegadores mais antigos
    ]
  }
  